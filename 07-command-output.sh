#1/bin/bash

# DATE=$( date +%F) # to run a command inside shell script --> $(command)

# echo "Today's date is: $DATE"

date    # command to print the output on the terminal
echo "the terminal time stamp is date"

#Timestamp=$(date)
echo "the time stamp is $(mkdir test1)" # $(command) syntax to run the command inside the shell script and get its value to the script.